{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "ai.apature/mcp-review",
  "description": "In-loop, read-only design review for coding agents: inspect a verified preview, return grounded findings with element refs and fixes, and recheck the agent's fix before CI.",
  "version": "1.1.0",
  "websiteUrl": "https://github.com/apatureai/mcp-review",
  "repository": {
    "url": "https://github.com/apatureai/mcp-review",
    "source": "github"
  },
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://mcp.apature.ai/mcp",
      "headers": [
        {
          "name": "Authorization",
          "description": "Bearer <token> or an OAuth-issued access token. There is no hosted endpoint today; the URL below is the shape a self-hosted deployment publishes. See the repository README.",
          "isRequired": true,
          "isSecret": true
        }
      ]
    }
  ],
  "_meta": {
    "ai.apature/status": "self-hosted. No public endpoint is operated today, so the URL in `remotes` documents the shape a deployment publishes rather than a live host. Run the server yourself (see the README) and replace the remote URL before submitting this listing to a registry.",
    "ai.apature/protocol_baseline": "2025-11-25",
    "ai.apature/catalog_version": "1.1.0",
    "ai.apature/read_only_boundary": "Customer-facing: the server judges, explains, and verifies. It never edits code, commits, pushes, or opens pull requests. Submit and recheck create metered jobs; design_review_get and design_review_panel_action are read-only, and a panel apply_fix returns the fix for the caller's coding agent rather than applying it.",
    "ai.apature/tools": [
      "design_review",
      "design_review_get",
      "design_recheck",
      "design_review_cancel",
      "design_review_panel_action"
    ]
  }
}
